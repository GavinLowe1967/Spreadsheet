https://retractionwatch.com/2013/04/18/influential-reinhart-rogoff-economics-paper-suffers-database-error/

Current
=======

Type checking
=============

*** Make "val" definition of concat have a type parameter argument. 

Typechecking concat in checkFunctionApp1 gets

res0 = FunctionType(
  List((A#0,AnyTypeConstraint)), List(ListType(TypeParam(A#0))),
  FunctionType(List(), List(ListType(ListType(TypeParam(A#0)))), 
    ListType(TypeParam(A#0))))

which is what we want.  But the use of subTypeParamsInResult, replaces the
TypeParam(A#0) by a TypeVar.

If we remove subTypeParamsInResult then the mkEmpty examples
(TypeCheckerTest2, l54-)

def mkEmpty[A](): List[A] = []; val xs = mkEmpty()
def mkEmpty[A <: Eq](): List[A] = []; val xs = mkEmpty()

give xs type ListType(TypeParam("A")), where "A" is never introduced; and
loses the Eq constraint in the latter case.  I think we'd like the latter to
have type [A <: Eq] ListType(TypeParam("A")), but there's currently no way to
say that in the type system.

Alternatively, require calls to mkEmpty to provide the type parameter,
e.g. val xs = mkEmpty[String].


Priorities
==========

Unify how Eq and Ord are implemented: former is in BinOpApply and latter is in
Value.scala.

Give better error message when formal type parameters are required but not
provided.  When used in a cell read type, or as a necessary type parameter,
but no formal value parameter uses that type.
def f[A <: CellExp]() = { def g[B <: CellExp]() = #A3: B; g[A] }
def f1[A <: CellType]() = { val x = #A3: A; 7 }; val y = f1()
- Store in TypeEnv those type parameters used in cell reads;
- In TypeChecker.typeCheckFnDecl, store that in the FunctionType object;
- In ExpTypeChecker, case of FunctionApp, check required type parameters
  provided.  And also update TypeEnv if actual type parameter used for
  necessary formal type parameter.

* In cell match constructs, allow type parameter as pattern. 


* improve error messages from Unification.scala

- Make sure #include-d files exist, or give error

- Try to remove ParseResult from Input.scala.  Input uses Failure.reset, so
  this might not be possible.  Need to provide Input with a parameter with a
  reset operation. 

- Undoing of operations.

- Use SplitPane in Frame.scala -- DONE; TODO: add to manual

-- More testing on haskell.dir, especially "val" definitions, via EvaluationTest.scala

- Improve AV script
* Change current to be list of candidate numbers. 

- Allow pattern matching on ::

- Allow "error" expression, with arbitrary type. 

- Allow more general function applications. 

- Anonymous functions.

- Export sheet and load sheet operations.

- Add libraries to manual.

---


X. Manual -- keep updated.

- Another example: admissions spreadsheet.

- Remove repetition in "Type error: Unknown type(s): A, A, A ...
in def foldr1(f: A => A => A)(xs: List[A]): A = { ... "

* println

3. Allow function that expects a tuple to be applied to a vector of
values; and vice versa, e.g. get1(x,y).

* Another example: admissions spreadsheet

* Tidy up FunctionAppTypeChecker.scala

* Extend arithmetic operators to, e.g. (Int,Float) => Float

* More general expressions on LHS of cell write directives.

* Expressions like Cells(#A, #0 to #5): List[Int]

* Catch stack overflows -- limit depth of recursion.

Low Priority
============

* Improve type error in Unification.unify.  Example: bas(foo) in
  TypeCheckerTest3.scala.  Gives error "Expected Int, found ((Int) => Int) =>
  Int"; should really be "Expected Int => Int, found (((Int) => Int) => Int)
  => Int".

* Allow typing on LHS of val declarations, e.g. "val x: Int = 3", "val x: Int
  = #A3".

* Improve error messages in Unification.unify, the case of FunctionTypes.

*Re-merge DeclarationTypeChecker and TypeChecker

* Allow function application without parentheses. 

* Improve treatment of Strings:
- Allow non-quoted strings in CSV
- Think about escaped characters in non-quoted string cells.

* Order relations on rows, columns

* Look again at Unification.scala.  Are all branches tested?

* In Environment, use null intead of Empty() (to save memory). 


======================================================================
======================================================================
